import 'package:flutter/material.dart';
import 'package:mess_app_staff/utils/api.dart';

class LaunchScreen extends StatelessWidget {
  final API? api;
  const LaunchScreen({super.key, this.api});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
